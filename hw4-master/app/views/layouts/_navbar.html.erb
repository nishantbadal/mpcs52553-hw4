<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-section" aria-expanded="false">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "StuffMart", "/", class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-collapse-section">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Sell Something!', "/products/new" %></li>
        <li><%= link_to 'User Stories', "/help" %></li>
      </ul>

      <form class="navbar-form navbar-left">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
      </form>

      <ul class="nav navbar-nav navbar-right">
        <% if session["user_id"].present? %>
          <% user = User.find_by(id: session["user_id"]) %>
          <p class="navbar-text">Hello, <%= user.name %></p>
        <% end %>

        <li><%= link_to "Sign Up", "/users/new" %></li>
        <li><%= link_to "Sign In", '/sessions/new' %></li>
        <li><%= link_to "Sign Out", '/logout', method: :delete %></li>
        <li><%= link_to "My Account", "/users/#{User.first.id}" %></li>
      </ul>

    </div>
  </div>
</nav>
